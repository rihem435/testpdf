import 'package:flutter/material.dart';

class ContratScreen extends StatelessWidget {
const ContratScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context){
    return Container();
  }
}